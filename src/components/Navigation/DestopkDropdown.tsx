import { NavigationLink } from "../../utils/contentTypes"


interface DestopkDropdownProps {
  links?: NavigationLink[]
}

const DestopkDropdown = (props: DestopkDropdownProps) => {
  return (
    <div>DestopkDropdown</div>
  )
}

export default DestopkDropdown